# This file was automatically generated.

[package]
edition = "2024"
name = "stm32l431-async"
version = "0.1.0"

[dependencies]
cortex-m = { version = "0.7.7", features = ["inline-asm", "critical-section-single-core"] }
cortex-m-rt = "0.7.5"
defmt = { version = "1.0.1", optional = true }
defmt-rtt = { version = "1.1.0", optional = true }
embassy-futures = "0.1.2"
embassy-stm32 = { version = "0.6.0", features = ["memory-x", "stm32l431rc", "time-driver-any", "exti", "unstable-pac"] }
embassy-executor = { version = "0.10.0", features = [
    "platform-cortex-m",
    "executor-thread",
    "executor-interrupt",
] }
embassy-sync = "0.8.0"
embassy-time = { version = "0.5.1", features = ["tick-hz-32_768"] }
panic-halt = "1.0.0"
panic-probe = { version = "1.0.0", features = ["print-defmt"], optional = true }


static_cell = { version = "2.1.1", features = [] }
icm20948 = { package = "icm20948-rs", path = "../..", features = ["async"] }
embedded-hal-bus = { version = "0.2.0", features = ["async"] }
embedded-hal = "1.0"
libm = "0.2"
heapless = "0.8"
ahrs = { version = "0.8", default-features = false }
nalgebra = { version = "0.34", default-features = false, features = ["libm"] }
micromath = "2.1"


[[bin]]
name = "wake_on_motion_async"
path = "src/wake_on_motion_async.rs"

[[bin]]
name = "hardware_interrupt_async"
path = "src/hardware_interrupt_async.rs"

[profile.dev]
debug = true
lto = true
opt-level = "z"
incremental = true

[profile.release]
debug = false
lto = true
opt-level = "z"
incremental = true

[features]
defmt = ["dep:defmt"]
defmt-rtt = ["dep:defmt-rtt"]
panic-probe = ["dep:panic-probe"]
default = ["debug"]
debug = [
    "defmt",
    "defmt-rtt",
    "panic-probe",
    "embassy-executor/defmt",
    "embassy-sync/defmt",
    "embassy-futures/defmt",
    "embassy-time/defmt",
    "embassy-time/defmt-timestamp-uptime",
    "embassy-stm32/defmt",
    "embedded-hal-bus/defmt-03",
    "embedded-hal/defmt-03",
    "icm20948/defmt",
]
